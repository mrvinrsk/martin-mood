import {defineConfig} from 'astro/config';

// https://astro.build/config

export default defineConfig({
    buildOptions: {
        baseUrl: '/', // Use this for localhost
    },
    // Other configuration options
});
